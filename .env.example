# ─── Fabric Gateway — Environment Variables ───
# Copy this file:  cp .env.example .env
# The defaults below work for local dev with `docker compose up`.

# ─── Server ───
PORT=3100
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=debug

# ─── Database (required) ───
DATABASE_URL=postgresql://fabric:fabric@localhost:5432/fabric

# ─── Redis (optional — gateway works without it) ───
REDIS_URL=redis://localhost:6379

# ─── CORS ───
CORS_ORIGIN=http://localhost:3000

# ─── Chain ───
CHAIN_RPC_URL=https://sepolia.base.org
CHAIN_ID=84532
USE_TESTNET=true

# ─── Contract Addresses (Base Sepolia defaults) ───
USDC_ADDRESS=0x036CbD53842c5426634e7929541eC2318f3dCF7e
FABRIC_IDENTITY_ADDRESS=0xa87df8b26393e48173530648a0baf7495bd6a66a
FABRIC_REGISTRY_ADDRESS=0x1ed343eec726ac3212d3ffe7c456617cf561b9f2

# ─── Wallet / Operator ───
# Private key for on-chain operations (fee collection, identity writes)
# NEVER commit a real private key — use env vars or a KMS in production
FABRIC_OPERATOR_KEY=
FABRIC_FEE_WALLET=0x0000000000000000000000000000000000000000
KMS_PROVIDER=none

# ─── API Key Prefix ───
API_KEY_PREFIX=fab

# ─── Stripe Billing (optional — not needed for local dev) ───
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_BUILDER=price_builder_monthly
STRIPE_PRICE_PRO=price_pro_monthly
STRIPE_PRICE_TEAM=price_team_monthly
STRIPE_OVERAGE_PRICE=price_overage_per_request

# ─── Brave Search (optional — enables web-search provider) ───
BRAVE_API_KEY=

# ─── Sentry (optional — error tracking) ───
SENTRY_DSN=
